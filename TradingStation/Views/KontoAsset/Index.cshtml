@model IEnumerable<TradingStation.Models.KontoAsset>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.assetAmount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.assetAmount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KontoAssetID }) |
            @Html.ActionLink("Details", "Details", new { id=item.KontoAssetID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KontoAssetID })
        </td>
    </tr>
}

</table>
